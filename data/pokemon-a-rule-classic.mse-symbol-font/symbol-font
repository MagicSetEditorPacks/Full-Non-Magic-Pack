mse version: 2.0.0
version: 2018-06-28
short name: normal
position hint: 001
icon: colorless.png
# Symbol font for energy

image font size: 53
horizontal space: 1
vertical space: 2
#Colorless Energy
symbol:
	code: C
	image: {if is_outline_rule() then "colorless-outline.png" else "colorless.png" }
	image font size: 53
#Dark Energy
symbol:
	code: D
	image: {if is_outline_rule() then "dark-outline.png" else "dark.png" }
	image font size: 53
#Lightning Energy
symbol:
	code: L
	image: {if is_outline_rule() then "lightning-outline.png" else "lightning.png" }
	image font size: 53
#Fighting Energy
symbol:
	code: F
	image: {if is_outline_rule() then "fighting-outline.png" else "fighting.png" }
	image font size: 53
#Fire Energy
symbol:
	code: R
	image: {if is_outline_rule() then "fire-outline.png" else "fire.png" }
	image font size: 53
#Grass Energy
symbol:
	code: G
	image: {if is_outline_rule() then "grass-outline.png" else "grass.png" }
	image font size: 53
#Metal Energy
symbol:
	code: M
	image: {if is_outline_energy() then "metal-outline.png" else "metal.png" }
	image font size: 53
#Psychic Energy
symbol:
	code: P
	image: {if is_outline_rule() then "psychic-outline.png" else "psychic.png" }
	image font size: 53
#Water Energy
symbol:
	code: W
	image: {if is_outline_rule() then "water-outline.png" else "water.png" }
	image font size: 53
#Dragon Energy
symbol:
	code: O
	image: {if is_outline_energy() then "dragon-outline.png" else "dragon.png" }
	image font size: 53
#Fairy Energy
symbol:
	code: Y
	image: {if is_outline_rule() then "fairy-outline.png" else "fairy.png" }
	image font size: 53
#Diamond
symbol:
	code: $
	image: {if is_outline_rule() then "$-Outline.png" else if is_two_tone_rule() then "$-TT.png" else if is_wt_rule() then "$-Wt.png" else "$-Bk.png"}
	image font size: 53
#Down Arrow
symbol:
	code: AR
	image: {if is_outline_rule() then "AR-Outline.png" else if is_two_tone_rule() then "AR-TT.png" else if is_wt_rule() then "AR-Wt.png" else "AR-Bk.png"}
	image font size: 65
#EX (BW/XY) Pokemon
symbol:
	code: XE
	image: {if is_outline_rule() then "XE-Outline.png" else if is_two_tone_rule() then "XE-TT.png" else if is_wt_rule() then "XE-Wt.png" else "XE-Bk.png"}
	image font size: 53
#Mega Pokemon
symbol:
	code: XM
	image: {if is_outline_rule() then "XM-Outline.png" else if is_two_tone_rule() then "XM-TT.png" else if is_wt_rule() then "XM-Wt.png" else "XM-Bk.png"}
	image font size: 53
#GX Pokemon
symbol:
	code: XG
	image: {if is_outline_rule() then "XG-Outline.png" else if is_two_tone_rule() then "XG-TT.png" else if is_wt_rule() then "XG-Wt.png" else "XG-Bk.png"}
	image font size: 53
#ex (EX Series) Pokemon
symbol:
	code: XS
	image: {if is_outline_rule() then "XS-Outline.png" else if is_two_tone_rule() then "XS-TT.png" else if is_wt_rule() then "XS-Wt.png" else "XS-Bk.png"}
	image font size: 53
#Galactic (Team Galactic) Pokemon
symbol:
	code: TG
	image: {if is_outline_rule() then "TG-Outline.png" else if is_two_tone_rule() then "TG-TT.png" else if is_wt_rule() then "TG-Wt.png" else "TG-Bk.png"}
	image font size: 53
#Gym Leader Pokemon
symbol:
	code: TL
	image: {if is_outline_rule() then "TL-Outline.png" else if is_two_tone_rule() then "TL-TT.png" else if is_wt_rule() then "TL-Wt.png" else "TL-Bk.png"}
	image font size: 53
#Ultra Prism Pokemon
symbol:
	code: UP
	image: {if is_outline_rule() then "UP-Outline.png" else if is_two_tone_rule() then "UP-TT.png" else if is_wt_rule() then "UP-Wt.png" else "UP-Bk.png"}
	image font size: 53
#Star Pokemon
symbol:
	code: #
	image: {if is_outline_rule() then "#-Outline.png" else if is_two_tone_rule() then "#-TT.png" else if is_wt_rule() then "#-Wt.png" else "#-Bk.png"}
	image font size: 53
#Elite 4 Pokemon
symbol:
	code: %
	image: {if is_outline_rule() then "%-Outline.png" else if is_two_tone_rule() then "%-TT.png" else if is_wt_rule() then "%-Wt.png" else "%-Bk.png"}
	image font size: 53
#Unown A
symbol:
	code: &A
	image: {if is_outline_rule() then "&A-Outline.png" else if is_two_tone_rule() then "&A-TT.png" else if is_wt_rule() then "&A-Wt.png" else "&A-Bk.png"}
	image font size: 53
#Unown B
symbol:
	code: &B
	image: {if is_outline_rule() then "&B-Outline.png" else if is_two_tone_rule() then "&B-TT.png" else if is_wt_rule() then "&B-Wt.png" else "&B-Bk.png"}
	image font size: 53
#Unown C
symbol:
	code: &C
	image: {if is_outline_rule() then "&C-Outline.png" else if is_two_tone_rule() then "&C-TT.png" else if is_wt_rule() then "&C-Wt.png" else "&C-Bk.png"}
	image font size: 53
#Unown D
symbol:
	code: &D
	image: {if is_outline_rule() then "&D-Outline.png" else if is_two_tone_rule() then "&D-TT.png" else if is_wt_rule() then "&D-Wt.png" else "&D-Bk.png"}
	image font size: 53
#Unown E
symbol:
	code: &E
	image: {if is_outline_rule() then "&E-Outline.png" else if is_two_tone_rule() then "&E-TT.png" else if is_wt_rule() then "&E-Wt.png" else "&E-Bk.png"}
	image font size: 53
#Unown F
symbol:
	code: &F
	image: {if is_outline_rule() then "&F-Outline.png" else if is_two_tone_rule() then "&F-TT.png" else if is_wt_rule() then "&F-Wt.png" else "&F-Bk.png"}
	image font size: 53
#Unown G
symbol:
	code: &G
	image: {if is_outline_rule() then "&G-Outline.png" else if is_two_tone_rule() then "&G-TT.png" else if is_wt_rule() then "&G-Wt.png" else "&G-Bk.png"}
	image font size: 53
#Unown H
symbol:
	code: &H
	image: {if is_outline_rule() then "&H-Outline.png" else if is_two_tone_rule() then "&H-TT.png" else if is_wt_rule() then "&H-Wt.png" else "&H-Bk.png"}
	image font size: 53
#Unown I
symbol:
	code: &I
	image: {if is_outline_rule() then "&I-Outline.png" else if is_two_tone_rule() then "&I-TT.png" else if is_wt_rule() then "&I-Wt.png" else "&I-Bk.png"}
	image font size: 53
#Unown J
symbol:
	code: &J
	image: {if is_outline_rule() then "&J-Outline.png" else if is_two_tone_rule() then "&J-TT.png" else if is_wt_rule() then "&J-Wt.png" else "&J-Bk.png"}
	image font size: 53
#Unown K
symbol:
	code: &K
	image: {if is_outline_rule() then "&K-Outline.png" else if is_two_tone_rule() then "&K-TT.png" else if is_wt_rule() then "&K-Wt.png" else "&K-Bk.png"}
	image font size: 53
#Unown L
symbol:
	code: &L
	image: {if is_outline_rule() then "&L-Outline.png" else if is_two_tone_rule() then "&L-TT.png" else if is_wt_rule() then "&L-Wt.png" else "&L-Bk.png"}
	image font size: 53
#Unown M
symbol:
	code: &M
	image: {if is_outline_rule() then "&M-Outline.png" else if is_two_tone_rule() then "&M-TT.png" else if is_wt_rule() then "&M-Wt.png" else "&M-Bk.png"}
	image font size: 53
#Unown N
symbol:
	code: &N
	image: {if is_outline_rule() then "&N-Outline.png" else if is_two_tone_rule() then "&N-TT.png" else if is_wt_rule() then "&N-Wt.png" else "&N-Bk.png"}
	image font size: 53
#Unown O
symbol:
	code: &O
	image: {if is_outline_rule() then "&O-Outline.png" else if is_two_tone_rule() then "&O-TT.png" else if is_wt_rule() then "&O-Wt.png" else "&O-Bk.png"}
	image font size: 53
#Unown P
symbol:
	code: &P
	image: {if is_outline_rule() then "&P-Outline.png" else if is_two_tone_rule() then "&P-TT.png" else if is_wt_rule() then "&P-Wt.png" else "&P-Bk.png"}
	image font size: 53
#Unown Q
symbol:
	code: &Q
	image: {if is_outline_rule() then "&Q-Outline.png" else if is_two_tone_rule() then "&Q-TT.png" else if is_wt_rule() then "&Q-Wt.png" else "&Q-Bk.png"}
	image font size: 53
#Unown R
symbol:
	code: &R
	image: {if is_outline_rule() then "&R-Outline.png" else if is_two_tone_rule() then "&R-TT.png" else if is_wt_rule() then "&R-Wt.png" else "&R-Bk.png"}
	image font size: 53
#Unown S
symbol:
	code: &S
	image: {if is_outline_rule() then "&S-Outline.png" else if is_two_tone_rule() then "&S-TT.png" else if is_wt_rule() then "&S-Wt.png" else "&S-Bk.png"}
	image font size: 53
#Unown T
symbol:
	code: &T
	image: {if is_outline_rule() then "&T-Outline.png" else if is_two_tone_rule() then "&T-TT.png" else if is_wt_rule() then "&T-Wt.png" else "&T-Bk.png"}
	image font size: 53
#Unown U
symbol:
	code: &U
	image: {if is_outline_rule() then "&U-Outline.png" else if is_two_tone_rule() then "&U-TT.png" else if is_wt_rule() then "&U-Wt.png" else "&U-Bk.png"}
	image font size: 53
#Unown V
symbol:
	code: &V
	image: {if is_outline_rule() then "&V-Outline.png" else if is_two_tone_rule() then "&V-TT.png" else if is_wt_rule() then "&V-Wt.png" else "&V-Bk.png"}
	image font size: 53
#Unown W
symbol:
	code: &W
	image: {if is_outline_rule() then "&W-Outline.png" else if is_two_tone_rule() then "&W-TT.png" else if is_wt_rule() then "&W-Wt.png" else "&W-Bk.png"}
	image font size: 53
#Unown X
symbol:
	code: &X
	image: {if is_outline_rule() then "&X-Outline.png" else if is_two_tone_rule() then "&X-TT.png" else if is_wt_rule() then "&X-Wt.png" else "&X-Bk.png"}
	image font size: 53
#Unown Y
symbol:
	code: &Y
	image: {if is_outline_rule() then "&Y-Outline.png" else if is_two_tone_rule() then "&Y-TT.png" else if is_wt_rule() then "&Y-Wt.png" else "&Y-Bk.png"}
	image font size: 53
#Unown Z
symbol:
	code: &Z
	image: {if is_outline_rule() then "&Z-Outline.png" else if is_two_tone_rule() then "&Z-TT.png" else if is_wt_rule() then "&Z-Wt.png" else "&Z-Bk.png"}
	image font size: 53
symbol:
	code: A!
	image: ability.png
	image font size: 170
##############################################################
# Insert-symbol menu
insert symbol menu:
	item: $
	item: #
	item: %
	item: &A
	item: &B
	item: &C
	item: &D
	item: &E
	item: &F
	item: &G
	item: &H
	item: &I
	item: &J
	item: &K
	item: &L
	item: &M
	item: &N
	item: &O
	item: &P
	item: &Q
	item: &R
	item: &S
	item: &T
	item: &U
	item: &V
	item: &W
	item: &X
	item: &Y
	item: &Z
	item: AR
	item: C
	item: D
	item: F
	item: G
	item: L
	item: M
	item: O
	item: P
	item: R
	item: W
	item: Y
	item: TG
	item: UP
	item: XE
	item: XG
	item: XM
	item: XS